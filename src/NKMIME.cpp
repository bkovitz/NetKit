#include <NetKit/NKMIME.h>

using namespace netkit;

const std::string mime::null;
const std::string mime::text::html( "text/html" );
const std::string mime::text::plain( "text/plain" );
const std::string mime::image::jpg( "image/jpg" );
const std::string mime::image::png( "image/png" );
const std::string mime::application::pdf( "application/pdf" );
const std::string mime::application::ipp( "application/ipp" );
